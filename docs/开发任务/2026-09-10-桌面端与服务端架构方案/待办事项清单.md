# 待办事项清单（恢复执行入口）

> 用途：每次继续开发、新开对话或换人接手时，**先读本文件**，再按下面入口文档推进。本文件只做进度快照与待办索引，具体任务证据回填在各自 `phase-*.md`。

## 一、任务目录与入口

- 任务根目录：`docs/开发任务/2026-09-10-桌面端与服务端架构方案/`
- 总清单（阶段状态表/待修订项/已确认决策）：`tasks.md`
- 架构图与判据表：`架构图.md`
- 技术方案：`design.md`
- Phase 文档：`phase-0` 到 `phase-8`（按编号）
- 业务复盘：`旧业务逻辑梳理.md`、`旧表字段覆盖核对表.md`、`页面迁移矩阵.md`
- 图标规格：`桌面端图标设计规格.md`
- 复盘核查清单（2026-09-16 整体复盘）：`复盘核查清单.md`

## 二、当前进度快照（2026-09-15）

| Phase | 状态 |
|---|---|
| 0 桌面端工程与基础壳 | ✅ 已完成 |
| 1 服务端用户与权限 | ✅ 已完成 |
| 2 Redis/BullMQ 任务与轻量工作流 | ✅ 已完成 |
| 3 RPA Agent | ✅ 已完成 |
| 4 AI、报告与下游工作流 | 进行中（4.1-4.6、4.8-4.12 已完成；**4.7 待真实 Key 实测**） |
| 5 资产与 StorageDriver | ✅ 已完成 |
| 6 打包更新与服务端部署 | 进行中（6.0/6.7/6.8 完成、6.5 CI 工作流已落地；6.1-6.4、6.6 待外部资源） |
| 7 历史功能全量迁移与收口 | 进行中（7.1-7.6、7.8-7.10 完成；7.7 待人工验收后直接删除旧代码） |
| 8 测试与最终验收 | pending |

## 三、下一步待办（按执行顺序）

### 复盘核查（2026-09-16）：发现的新待办

- [ ] **[待人工决策] R-1 桌面端 RPA 采集执行链未接线**：`apps/desktop/src/main/` 下 `collection-runner.ts`/`agent-runtime.ts`/`collection-modes.ts`/`result-store.ts`/`sync-config.ts`/`python-runner.ts`/`chrome.ts` 均写好，但主进程 `index.ts` 只注册 `store` 与 `local-db` handler，无任何入口引用采集链（grep 已证实）。Phase 3 标注完成但桌面采集端未接到 Electron 主进程；是 Phase 8.3 端到端联调前置阻塞。需决策：接通采集链 IPC，或确认是否本就在其它任务推进。
- [ ] R-2 主图/详情图提示词详细规则（SLOT/DETAIL）搬运（`apps/legacy/src/server/mainImagePromptExpansion.js` → ImagePrompt 服务）
- [ ] R-3 报告价格带动态聚类算法实现（旧 `aiMarketAnalysis.js`，现为等宽分带）
- [ ] R-4 前端占位 StubPage 接真（看板/市场报告/数据下载/图片生成/详情图/一键复刻/商品主档/数据 Agent/报告详情等，后端接口多已就绪）

### 任务梳理（2026-09-16）：还能做的

- [ ] Phase 8.1 服务端 Jest 测试（Guard/DTO/队列/FlowProducer/Agent 租约/StorageDriver/状态机）
- [ ] Phase 8.2 桌面端 Vitest/Playwright 测试（SQLite 清理/空间上限/CDP 登录引导/UI E2E）
- [ ] Phase 8.3 端到端联调（桌面端→服务端→AI(mock)→报告→生图→审核→发布）
- [ ] Phase 8.4 多 Provider Profile、安全与切换最终验收
- [ ] Phase 7.1 剩余页面迁移（数据 Agent、看板、商品/平台详情、设置详情等按 `页面迁移矩阵.md`）
- [ ] Phase 7.2 提示词详细规则与价格带动态聚类算法迁移
- [ ] Phase 6.1 本地内测包演练（`pnpm --filter desktop build:package` 复验 + 干净机器安装）
- [ ] Phase 6.4 旧环境变量/服务切换准备文档与脚本收口

### 待人工/外部资源（不阻塞，已登记）

- [ ] 4.7 真实 OpenRouter/淘宝 Key 限流实测
- [ ] 6.2 阿里云部署、6.3 备份回滚演练
- [ ] 6.5 Actions 真实运行 + Windows 虚拟机冒烟
- [ ] 6.6 正式图标与签名证书
- [ ] 自动更新源最终选型、OSS 厂商、域名/Nginx/证书、Actions secrets
- [ ] 7.7 人工本地验收后 `pnpm legacy:cleanup --apply` 直接删除旧代码

### 当前：Phase 5 资产与 StorageDriver

- [x] 5.1 实现 StorageDriver 抽象与本地目录驱动
- [x] 5.2 实现预签名上传与上传确认接口（StorageDriver，厂商待补）
- [x] 5.3 实现 generated_assets 记录与鉴权读取接口
- [x] 5.4 实现孤儿对象与失败/重试文件清理
- [x] 5.5 迁移旧本地资产生成物（public/generated 等）
- [x] 5.6 校验 StorageDriver 环境路由与本地/线上边界
- [x] 5.7 落地本地存储默认与可选同步开关（`sync.asset`）

### 当前：Phase 7 历史功能迁移与收口

- [~] 7.1 前端页面全量迁移（首批壳/登录/采集/报告/资产/管理列表已接 API，其余页面按 `页面迁移矩阵.md` 推进）
- [~] 7.2 服务端旧逻辑全量迁移（11 个旧文件已映射到新模块，提示词规则/动态聚类待续）
- [x] 7.3 旧表字段语义覆盖核对（不迁移数据）
- [x] 7.4 本地资产与文件迁移
- [x] 7.5 配置、Python 运行时与桌面端收口
- [x] 7.6 旧服务下线与切换
- [~] 7.7 清理旧应用依赖与运行目录（dry-run 通过，`--apply` 直接删除待人工验收后执行）
- [x] 7.8 关闭旧执行入口并完成单一实现收口
- [x] 7.9 迁移图片编辑与本地内容工具
- [x] 7.10 迁移视频复刻/视频图库页面与资产

### 当前：Phase 6 打包更新与服务端部署

- [x] 6.0 明确双轨打包节奏与 Phase 6 后置执行顺序
- [ ] 6.1 完成桌面端安装包（含 Python）与自动更新配置占位演练（当前任务）
- [ ] 6.2 落地服务端生产部署（阿里云 Nginx + PM2 + MySQL + Redis/BullMQ Worker + StorageDriver）
- [ ] 6.3 完成服务端备份与回滚预案
- [ ] 6.4 完成旧环境变量与旧服务切换准备
- [~] 6.5 配置 GitHub Actions 多平台打包并完成 Windows 冒烟（工作流已落地，运行待仓库 push + secrets；Windows 冒烟待虚拟机）
- [ ] 6.6 接入正式应用图标与签名资源
- [x] 6.7 补齐 dev/prod 环境变量注释与线上配置对照
- [x] 6.8 盘点线上/人工事项并逐项回填

### 之后：Phase 8 测试与最终验收

## 四、待人工/外部依赖清单（不阻塞开发的要提前登记，避免遗忘）

- [ ] **人工本地验收与旧代码清理（Phase 7.7）**：本地起项目走通核心流程（登录/采集/报告/资产/打包）后，人工确认无旧代码依赖（不归档、直接删除，避免误导），再执行 `pnpm legacy:cleanup --apply` 删除旧 `apps/legacy/` 与 `apps/backend/server.js`。
- [ ] **4.7 真实 AI/淘宝 Key**：OpenRouter、淘宝开放平台 AppKey/Secret/Session；字段已占位到 `apps/backend/.env.example`，账号回填后做限流/超时/错误码实测并回填 ProviderProfile（火山引擎 Ark 已停用，不再需要）。
- [ ] **OSS 厂商/bucket/region**：线上 bucket、dev/prod 划分、预签名域名；本地先走 LocalStorageDriver。
- [ ] **Windows 虚拟机**：mac 上装 Windows 虚拟机后补 Windows 冒烟（安装/启动/登录/RPA CDP）。
- [ ] **应用图标**：设计师交付 `icon.icns`/`icon.ico`/多尺寸 PNG 后接入 electron-builder。
- [ ] **自动更新源选型**：GitHub Releases / 阿里云静态源待专门讨论。
- [ ] **正式签名证书**：macOS Developer ID、Windows 签名证书。
- [ ] **域名/Nginx/HTTPS 证书**：上线前准备线上域名与证书。
- [ ] **GitHub Actions secrets / Releases token**：线上打包发布前配置。
- [ ] **环境变量真实值**：`apps/backend/.env` 与 `apps/desktop/.env` 的 DB/Redis/JWT/AI/OSS/更新源等按 `.env.example` 注释回填。

## 五、执行规则

- 一次推进一个原子任务：先标 `[~]`，改完用真实可运行验证（smoke/curl/build），通过才标 `[x]` 并回填证据。
- 禁止为了取证反复触发真实线上爬虫或大批量真实付费 AI 调用；优先 `rpa_runs/` 离线样本与 Mock。
- 发现设计/架构图与实现冲突时，先同步修订 `design.md`/`架构图.md` 再继续。
- Phase 6 打包部署后置；线上事项无论人工还是 AI 做，都登记到本文第四节与 phase-6「本地做/线上做清单」。
- 打包分两级：先本地内测包确认用户意向，再线上正式包。
