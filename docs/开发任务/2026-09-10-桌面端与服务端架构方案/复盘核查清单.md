# 复盘核查清单（2026-09-16）

> 目的：对 `2026-09-10-桌面端与服务端架构方案` 做一次整体复盘，核对方案推进状态、旧代码迁移完成度与文档一致性，发现的问题按「可修 / 待人工 / 外部依赖」三类登记。本清单为复盘快照，状态会随后续执行更新。
> 触发：git 目录重构（迁移到 `apps/*`）后的全量核查。

---

## 一、总览：各 Phase 推进状态（与文档标注核对）

| Phase | 文档标注状态 | 代码核实结论 | 是否与文档一致 |
|---|---|---|---|
| 0 桌面端工程 | ✅ 完成 | 一致 | ✅ |
| 1 用户与权限 | ✅ 完成 | 一致 | ✅ |
| 2 队列与 SSE | ✅ 完成 | 一致 | ✅ |
| 3 RPA Agent | ✅ 完成 | **后端侧完成；桌面端采集执行链未接线（见问题 R-1）** | ⚠️ 高估 |
| 4 AI/报告 | 进行中（4.7 待真实 Key） | 一致 | ✅ |
| 5 资产/存储 | ✅ 完成（OSS 驱动占位） | 一致 | ✅ |
| 6 打包部署 | 进行中（依赖外部资源） | 一致 | ✅ |
| 7 历史迁移收口 | 进行中（7.7 待删旧代码） | 一致 | ✅ |
| 8 测试验收 | pending | 一致 | ✅ |

> 说明：文档标注与实际代码基本相符，唯一高估项是 Phase 3（见问题 R-1）。

---

## 二、可修项（本次已修复 ✅）

| # | 问题 | 修复动作 | 状态 |
|---|---|---|---|
| R-0 | **方案文档路径大面积停留在旧扁平命名**（`backend/`、`frontend/`、`desktop/`、`app/`），与迁移后的 `apps/*` 结构不符，影响接手定位 | 统一替换为 `apps/backend`、`apps/frontend`、`apps/desktop`、`apps/legacy`（负前瞻避免双前缀）；`src/app/pages` 等 legacy 内层真实目录保持 `apps/legacy/src/app/pages` 不误改；日志文件名/级别语境（`app.log`/`error.log`/`debug.log`）还原为 `apps/backend/logs` 语义 | ✅ 已修 |

> R-0 涉及 `tasks.md`、`design.md`、`phase-0/3/4/5/6/7`、`待办事项清单`、`旧业务逻辑梳理`、`服务端旧逻辑迁移核对表`、`页面迁移矩阵`、`桌面端图标设计规格` 共 14 个文件。已逐项核对：无双前缀、无 `src/app` 内层误伤、无日志语境误改。

---

## 三、待人工确认（需你决策，不擅自动手）

| # | 问题/决策点 | 现状 | 建议 |
|---|---|---|---|
| R-1 | **桌面端 RPA 采集执行链是孤儿模块**：`apps/desktop/src/main/` 下 `collection-runner.ts`、`agent-runtime.ts`、`collection-modes.ts`、`result-store.ts`、`sync-config.ts`、`python-runner.ts`、`chrome.ts` 全部写好，但主进程 `index.ts` 只注册了 `store` 和 `local-db` 两组 handler，**没有任何入口引用采集链**（已全 src grep 证实）。即桌面端「拉起 Chrome/Python 采集」的链路未接到 Electron 主进程 | Phase 3 标注"完成"但桌面采集端未接线；端到端联调（Phase 8.3）前置阻塞 | 决定：接通采集链 IPC handler（功能开发），或先明确桌面端采集是否本就在另一任务推进 |
| R-2 | **主图/详情图提示词详细规则（SLOT/DETAIL 模块顺序）未搬运**：`apps/legacy/src/server/mainImagePromptExpansion.js` 标 ⏳，待按 Phase 7.2 逐条搬入 ImagePrompt 服务 | 服务端旧逻辑迁移核对表"待续项" | Phase 7.2 推进时搬运 |
| R-3 | **报告价格带动态聚类算法未实现**：现为等宽分带，旧 `aiMarketAnalysis.js` 的动态聚类算法待按 7.3 对照旧表语义回填 | 服务端旧逻辑迁移核对表"待续项" | Phase 7.3 实现 |
| R-4 | **前端大量路由仍为占位 StubPage**：`apps/frontend/src/main.tsx:33-47` 中 `/analytics`（数据看板）、`/analysis/market-reports`（市场报告）、`/data/downloads`（数据下载）、`/content/product-sets`（图片生成）、`/content/aplus`（详情图）、`/content/one-click-replicate`（一键复刻）、`/products/master-data`、`/products/management`、`/analysis/agent`（数据 Agent）、`/analysis/reports/:id`（报告详情）等均为 StubPage；后端接口大多已就绪 | 页面迁移矩阵标 ⏳ | 按矩阵逐个从占位路由接真页面 |
| R-5 | **旧代码删除待人工验收**：`apps/legacy/` 与 `apps/backend/server.js` 按 `scripts/cleanup-legacy.mjs` 待删；Phase 7.7 未执行。`server.js` 是旧单体 mock/托管入口，与新 NestJS `main.ts` **互斥占同一端口 8787，不可同时运行** | 计划内待删项 | 本地走通核心流程（登录/采集/报告/资产/打包）并确认无旧依赖后，执行 `pnpm legacy:cleanup --apply` |

---

## 四、外部依赖 / 不阻塞本地开发（已登记待办清单）

| # | 事项 | 阻塞点 |
|---|---|---|
| X-1 | Phase 4.7 限流/超时/错误码实测 | 需真实 OpenRouter/淘宝 Key；火山 Ark 已停用 |
| X-2 | `OssStorageDriver` 为占位，`STORAGE_DRIVER=oss` 时读写抛"待接入" | OSS 厂商/bucket 未定，靠 `LocalStorageDriver` 兜底 |
| X-3 | 阿里云部署（Nginx+PM2）、备份回滚演练 | 域名/Nginx/证书/服务器 |
| X-4 | GitHub Actions 多平台打包真实运行 + Windows 虚拟机冒烟 | Actions secrets、Windows 环境 |
| X-5 | 正式应用图标与签名证书 | 设计师交付、证书 |
| X-6 | 自动更新源选型 | 更新服务器/仓库选择 |
| X-7 | Node 25 下 Prisma `migrate dev` 报 `Schema engine error: undefined` | 已用 `migrate diff + db execute` 绕过，待升级后回归 |

---

## 五、后续推进建议（按优先级）

1. **接通桌面端采集链**（R-1）：Phase 3 真正收口 + Phase 8.3 端到端联调前置。需你先定方向。
2. **Phase 8 测试收尾**：服务端 Jest + 桌面端 Vitest/Playwright + 端到端联调（Agent→服务端→AI mock→报告→生图→审核→发布）。
3. **Phase 7.2/7.3**：搬运提示词规则 + 实现价格带动态聚类算法（R-2、R-3）。
4. **前端 Stub 页面接真**（R-4）：按页面迁移矩阵推进。
5. **文档路径统一已完成**（R-0），无需再动。

---

## 附：本次核查证据来源

- 文档：`tasks.md`、`design.md`、各 `phase-N-*.md`、`待办事项清单.md`、`旧业务逻辑梳理.md`、`旧表字段覆盖核对表.md`、`服务端旧逻辑迁移核对表.md`、`页面迁移矩阵.md`
- 代码：`apps/desktop/src/main/*`（index/chrome/agent-runtime/python-runner/collection-runner）、`apps/backend/server.js`、`apps/frontend/src/main.tsx`、`apps/legacy/src/app/pages/`、`apps/legacy/src/server/`、`scripts/cleanup-legacy.mjs`
- 核对点：桌面采集链无入口引用（grep）、server.js 无 `/api/rpa`、`/api/ide` 端点、前端 StubPage 路由清单、迁移矩阵 ⏳ 项、schema-coverage-smoke（mappings:21 / modelCount:35 / missingModels:[]）。
