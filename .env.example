# ===== 根目录说明（不是配置事实源）=====
# 服务端运行时统一读取 backend/.env，完整示例见 apps/backend/.env.example
# 桌面端运行时/脚本读取 desktop/.env，完整示例见 apps/desktop/.env.example
# 根目录不重复维护一份变量清单，避免本地/线上配置多个事实源。
# 真实密钥（AI Key、淘宝 Key、JWT/DB 密码、OSS Secret）只写入各自的 .env，不提交 git。
